# Birthday.exe

A small static birthday website based on the provided plan.

Open `index.html` in a browser to use it. The access code is `1016`.

To customize the letter, code, or gift options, edit `index.html` and `script.js`.
